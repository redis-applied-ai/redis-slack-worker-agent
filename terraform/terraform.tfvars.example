# Example Terraform variables file
# Copy this to terraform.tfvars and update the values

# Minimal configuration for quick start
aws_region   = "us-east-1"
project_name = "my-ai-agent"           # Choose a unique name (reserved: applied-ai-agent-worker)

# S3 bucket must be globally unique
bucket_name  = "my-ai-agent-content-1234"
